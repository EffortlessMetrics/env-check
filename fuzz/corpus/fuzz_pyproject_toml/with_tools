[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "myproject"
requires-python = ">=3.9"

[tool.black]
line-length = 88

[tool.pytest]
testpaths = ["tests"]
